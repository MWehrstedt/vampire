{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            "command": "cmd.exe",
            "args": [
                "/c",
                "SET COMPILER_DIR=..\\..\\Compiler && SET PATH=%COMPILER_DIR%\\WINDOWS\\Other Utilities;%COMPILER_DIR%\\WINDOWS\\bin;%PATH% && make re"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
