#ifndef __ZOMBIE_H_
#define __ZOMBIE_H

/// @brief Zombie draw routine
/// @param idx Index in active enemy slots
void drawZombie(short idx);

/// @brief Zombie update routine
/// @param idx Index in active enemy slots
void updateZombie(short idx);
#endif